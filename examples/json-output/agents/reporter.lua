return {
    name = "reporter",
    goal = "Take JSON data and produce formatted reports or save to files",
    capabilities = { "writing", "formatting", "file" },
    tools = { "file_write" },
    temperature = 0.3,
}
